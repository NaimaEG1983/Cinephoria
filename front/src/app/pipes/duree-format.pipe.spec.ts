import { DureeFormatPipe } from './duree-format.pipe';

describe('DureeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DureeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
